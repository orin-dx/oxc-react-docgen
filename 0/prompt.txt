<task-notification>
<task-id>wz2gedyag</task-id>
<tool-use-id>toolu_01Cq9hEsmtZ1jVuTtYmRwKRi</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-gabe-Projects-oxc-react-codegen/7bd18e20-7f86-4367-ae2f-7137cddc2b77/tasks/wz2gedyag.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Adversarial review of oxc-react-docgen for code quality, maintainability, DX, and performance" completed</summary>
<result>{"confirmed":[{"file":"crates/core/src/resolver/alias.rs","line":205,"dimensions":["quality","maintainability"],"severity":"high","summary":"resolve_base_as_chain's wildcard arm silently drops props with no diagnostic for every CollectedType variant not explicitly matched, violating both the 'no wildcard on project-local enums' style rule and the 'always emit a Diagnostic when degrading' non-negotiable.","detail":"resolve_base_as_chain (alias.rs:130-206) matches CollectedType::{Named, AtFile, Intersection, Object, Union} explicitly, then falls through `_ =&gt; ResolvedChain::default()` for everything else — IndexedAccess, Function, Conditional, Mapped, TemplateLiteral, KeyOf, TypeOf, Raw, and all primitive variants. This function is reached for every member of an Intersection/Union alias and for Omit/Pick/Partial/Required's `base`. A real-world pattern like `type Props = SomeGeneric['variant'] &amp; { asChild?: boolean }` (an IndexedAccess used as one intersection member — a plausible real shape, not contrived) silently contributes zero props and zero diagnostics for that member: the component's props chain quietly loses whatever `SomeGeneric['variant']` would have added, and nothing in the diagnostics output tells the caller why. This is exactly the pattern the project's own non-negotiables list (#6, #7) exist to prevent, and CollectedType is exactly the kind of actively-growing enum (TemplateLiteral/KeyOf/Mapped/Conditional/IndexedAccess were all added incrementally in prior sessions) where a silent catch-all quietly absorbs future variants too.","suggested_fix":"Replace the wildcard with an exhaustive match; for variants that genuinely aren't chain-providers (primitives, Raw, TypeOf, KeyOf, etc.) emit an Info/Warning diagnostic and return ResolvedChain::default() explicitly so future variants force a compile-time decision instead of silently matching the fallback.","area":"resolver"},{"file":"crates/core/src/known.rs","line":226,"dimensions":["quality"],"severity":"medium","summary":"PropsWithChildren/PropsWithRef's KnownPatternResult::Alias stringifies the inner PropType via raw_string() and the consumer re-wraps the WHOLE string (including any '&lt;...&gt;' generic args) as a bare type name instead of re-parsing it, breaking resolution whenever the inner type is itself generic.","detail":"known.rs:226/230 do `args.first().map(|inner| KnownPatternResult::Alias { name: inner.raw_string() })`. For a non-generic inner type (the common case, e.g. `PropsWithChildren&lt;ButtonProps&gt;`) raw_string() returns the bare name and this works. But for `PropsWithChildren&lt;SelectProps&lt;Option&gt;&gt;` (a parameterized inner type), raw_string() produces the display string \"SelectProps&lt;Option&gt;\" (types/output.rs:259-262), and both consumers of KnownPatternResult::Alias treat the whole string as a literal type name: named.rs:85 does `CollectedType::Named { name: alias_name.as_str().into(), args: vec![] }` and chain.rs:149 does `resolve_props_chain(&amp;name, &amp;[], ...)` — in both cases the name becomes the literal string \"SelectProps&lt;Option&gt;\" with the angle brackets baked in, which can never match any real import/interface/alias key (those are keyed on bare identifiers). The type silently degrades to an unresolvable Named/Opaque with a diagnostic naming a garbage identifier, instead of correctly delegating to SelectProps with Option substituted.","suggested_fix":"Have props_with_children/props_with_ref return the structured PropType (or re-derive a proper CollectedType::Named{name, args} from it) instead of a stringified KnownPatternResult::Alias, or extend the Alias variant to carry args separately so callers can reconstruct a real Named reference.","area":"resolver"},{"file":"crates/core/src/resolver/chain.rs","line":177,"dimensions":["performance"],"severity":"medium","summary":"resolve_props_chain deep-clones the whole matched CollectedInterface (including its Vec&lt;RawProp&gt; of recursive CollectedType trees) on every interface reference, even though resolve_interface_chain only needs a borrow.","detail":"`lookup_interface(&amp;ctx.global, ...).cloned()` clones the entire CollectedInterface every time any interface is referenced as a props type or extends target. Nothing mutably aliases ctx.global during the call — resolve_interface_chain (chain.rs:199) takes `iface: &amp;CollectedInterface`, and state (the only &amp;mut) is a disjoint object — so the clone isn't required for borrow-checking. primitives.rs:142's `lookup_interface_including_ambient` call proves this: it holds the returned `&amp;CollectedInterface` reference across further resolution calls with the same ctx/state shapes, with no clone at all. extends.rs:120 has the identical unnecessary `.clone()` for the same reason. For a widely-extended shared base interface (a common pattern in real component libraries), this multiplies a deep clone of a potentially large, recursive prop-type tree by every reference site.","suggested_fix":"Drop the .cloned()/.clone() and thread the borrowed &amp;CollectedInterface through, matching the pattern already used in primitives.rs's lookup_interface_including_ambient call site.","area":"resolver"},{"file":"crates/core/src/resolver/extends.rs","line":120,"dimensions":["performance"],"severity":"medium","summary":"real_html_attrs_chain clones the matched CollectedInterface for the same avoidable reason as chain.rs:177.","detail":"`ctx.global.interfaces.get(&amp;qualified_key).or_else(...).?.clone()` — same unnecessary deep clone as chain.rs:177, on the Full-mode HTML-attributes resolution hot path, which runs once per extends-a-builtin-HTML-element component.","suggested_fix":"Same as chain.rs:177 — hold the borrow instead of cloning.","area":"resolver"},{"file":"crates/core/src/resolver/named.rs","line":100,"dimensions":["maintainability"],"severity":"low","summary":"The 'silently-pass-through, no-diagnostic' set of TypeScript utility-type/HTML-attribute names is independently duplicated across three files with no shared constant.","detail":"known.rs:117 returns None for `Partial|Required|Readonly|NonNullable|Omit|Pick`; chain.rs:96-115 has its own overlapping-but-not-identical list (`Omit|Pick|Partial|Required|Readonly|NonNullable|ReturnType|Parameters|Awaited|Extract|Exclude|Record|ReadonlyArray|Array|Promise`); named.rs:100-122 has yet another superset (all of the above plus `object|Object|Function|Symbol|BigInt` and every React HTML-attribute type name). Each is a separate hand-maintained match arm. There's no compile-time link between them, so adding a new silently-handled utility type (or renaming/removing one) requires knowing to touch up to three files, and it's easy to update one and forget another, producing inconsistent behavior between chain-level and type-level resolution of the same type name.","suggested_fix":"Extract a single shared slice/const (or a small helper fn) for the TS-utility-type name set and reuse it from chain.rs, named.rs, and known.rs.","area":"resolver"},{"file":"crates/core/src/resolver/primitives.rs","line":57,"dimensions":["quality"],"severity":"low","summary":"resolve_intersection's `(string &amp; {})` special case is dead weight — fully subsumed by the general single-member branch immediately below it.","detail":"`if non_empty.len() == 1 &amp;&amp; matches!(non_empty[0], CollectedType::String) { return PropType::String; }` (lines 57-59) can never produce a result different from the very next branch, `if non_empty.len() == 1 { return resolve_collected_type(non_empty[0], ...); }` (lines 60-62), since resolve_collected_type maps CollectedType::String to PropType::String directly (collected.rs:38). The special case predates the general one having been generalized (both landed in the same June 15 refactor commit, ecacb3b6, per git blame) and is now redundant — harmless, but its presence implies String needs special handling when it no longer does, which will mislead the next person reading this function.","s
... (truncated 70735 chars, full result in /private/tmp/claude-501/-Users-gabe-Projects-oxc-react-codegen/7bd18e20-7f86-4367-ae2f-7137cddc2b77/tasks/wz2gedyag.output)</result>
<diagnostics>Per-agent results: /Users/gabe/.claude/projects/-Users-gabe-Projects-oxc-react-codegen/d83be67f-8b07-459c-989b-529791ef5ca7/subagents/workflows/wf_1e3ec847-131/journal.jsonl — one {"type":"result",...} line per completed agent with its full return value.
If the result above is empty or unexpected, Read this file BEFORE diagnosing — do not assume agents returned non-empty results.
To re-run with edited post-processing: Workflow({scriptPath: '/Users/gabe/.claude/projects/-Users-gabe-Projects-oxc-react-codegen-apps-validate/d83be67f-8b07-459c-989b-529791ef5ca7/workflows/scripts/adversarial-quality-review-wf_1e3ec847-131.js', resumeFromRunId: 'wf_1e3ec847-131'}) — agents whose (prompt, opts) are unchanged replay from cache.</diagnostics>
<usage><agent_count>143</agent_count><agents_done>143</agents_done><agents_error>0</agents_error><agents_skipped>0</agents_skipped><agents_empty_result>0</agents_empty_result><subagent_tokens>4263025</subagent_tokens><tool_uses>775</tool_uses><duration_ms>866698</duration_ms></usage>
</task-notification>