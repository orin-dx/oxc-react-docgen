{
  "name": "oxc-react-codegen",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "build": "moon run :build",
    "test": "moon run :test",
    "lint": "moon run :lint"
  }
}
