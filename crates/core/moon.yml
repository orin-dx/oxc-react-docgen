language: rust

tasks:
  build:
    command: cargo build -p oxc-react-docgen-core
    options:
      runFromWorkspaceRoot: true
    inputs:
      - "src/**/*"
      - "Cargo.toml"

  test:
    command: cargo nextest run -p oxc-react-docgen-core --locked
    options:
      runFromWorkspaceRoot: true
    inputs:
      - "src/**/*"
      - "tests/**/*"
      - "Cargo.toml"

  lint:
    command: cargo clippy -p oxc-react-docgen-core --locked -- -D warnings
    options:
      runFromWorkspaceRoot: true
    inputs:
      - "src/**/*"
      - "Cargo.toml"
