<task-notification>
<task-id>abeb84b2fca990749</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-gabe-Projects-oxc-react-codegen/c1a2ed3d-6a2c-4bd4-8b39-b490075d71ff/tasks/abeb84b2fca990749.output</output-file>
<status>completed</status>
<summary>Agent "Build react-resizable-panels fixture" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All verified. Final report:

## Summary

**Files created** (all under `/Users/gabe/Projects/oxc-react-codegen/fixtures/react-resizable-panels/`):
- `Panel.tsx` (144 lines) — from https://github.com/bvaughn/react-resizable-panels/blob/main/lib/components/panel/Panel.tsx
- `usePanelImperativeHandle.ts` (47 lines) — from https://github.com/bvaughn/react-resizable-panels/blob/main/lib/components/panel/usePanelImperativeHandle.ts
- `types.ts` (173 lines) — from https://github.com/bvaughn/react-resizable-panels/blob/main/lib/components/panel/types.ts

**Path correction**: confirmed via GitHub API that source now lives at `lib/components/panel/` (not `packages/`), matching the task's hint.

**Important structural finding**: the real, current `Panel` is **not** `React.forwardRef`. It's a plain function component taking a custom-named `panelRef` prop (`Ref&lt;PanelImperativeHandle | null&gt;`) plus a separate `elementRef` prop for the DOM node — both ordinary props, neither the special `ref`. `usePanelImperativeHandle` internally does `useImperativeHandle(panelRef, () =&gt; imperativePanelRef.current, [])` verbatim. I preserved this real structure rather than the forwardRef pattern the task description assumed, and flagged it in the file header comments.

**Trimmed/stubbed**: `useGroupContext` (parent Group registration/layout), `useSyncExternalStore` style subscription, `useMergedRefs`/`useStableCallback`/`useStableObject` (inlined trivial stand-ins), `getImperativePanelMethods` (real methods reach into live group state — replaced with no-ops), `RegisteredPanel`/`PanelConstraints` types. `PanelImperativeHandle` and `PanelProps` kept verbatim/near-verbatim.

**Test results** (`./target/release/oxc-react-docgen extract --src fixtures/react-resizable-panels --format canonical`): exit 0, no crash, 1 component, 1 warning. `Panel` is detected and all 12 explicitly-declared props extract correctly. `panelRef` shows up as `{"kind":"ref","element":"PanelImperativeHandle | null"}` — the imperative-handle type name is surfaced but not expanded into its methods. Real react-docgen-typescript (v2.4.0, verified via throwaway script, deleted after) behaves the same way: `panelRef.type.name = "Ref&lt;PanelImperativeHandle | null&gt; | undefined"` — a string signature, no method expansion. So absence of expanded methods is correct/expected parity with RDT.

**Real gap discovered**: `types.ts` uses `import type { HTMLAttributes } from "react"` (the actual bvaughn import style). Our tool can't resolve this named type-only import — it resolves `react` to its JS entry point rather than `@types/react`, leaving `composes: ["HTMLAttributes"]` opaque and dropping ~275 inherited HTML attributes that RDT correctly expands (287 vs. our 12 props). All 15 existing fixtures use `React.HTMLAttributes` namespace access, which works fine — so this fixture, by faithfully reproducing the real import style, surfaces a genuine, previously-uncovered resolution gap. Not fixed (out of scope per instructions), just reported.</result>
<usage><subagent_tokens>75141</subagent_tokens><tool_uses>36</tool_uses><duration_ms>341594</duration_ms></usage>
</task-notification>