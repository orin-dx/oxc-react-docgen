<task-notification>
<task-id>w6arlas5y</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-gabe-Projects-oxc-react-codegen/4fe08305-17f4-42f0-993c-45f9fae2f7a4/tasks/w6arlas5y.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Deep research harness — fan-out web searches, fetch sources, adversarially verify claims, synthesize a cited report." completed</summary>
<result>{"question":"How does moon 2.x vcs hooks work? Specifically: does it require lefthook or can it install git hooks natively without lefthook? What is hookFormat \"bash\" vs \"native\"? What does moon sync vcs-hooks actually install into .git/hooks/? Source: https://moonrepo.dev/docs/guides/vcs-hooks and https://moonrepo.dev/docs/config/workspace","summary":"Moon 2.x manages VCS hooks entirely natively — no lefthook or external tool is required. Running `moon sync vcs-hooks` generates hook scripts into `.moon/hooks/` and sets Git's `core.hooksPath` config to point there; nothing is written to `.git/hooks/` directly (a breaking change from v1.x). The `hookFormat` setting (added in v1.29.0, valid in 2.x) controls script format: `native` (default) emits Bash `.sh` on Unix and PowerShell `.ps1` on Windows, while `bash` forces Bash on all platforms. Hook commands are declared in `.moon/workspace.yml` under `vcs.hooks`, keyed by kebab-cased Git hook names (e.g., `pre-commit`), and the automatic sync behavior is controlled by `vcs.sync` (renamed from `vcs.syncHooks` in the v2.0 migration).","findings":[{"claim":"Moon 2.x does not require lefthook or any external hook management tool. It provides a built-in solution that manages git hooks natively via its own `vcs.hooks` configuration in `.moon/workspace.yml`.","confidence":"high","sources":["https://moonrepo.dev/docs/guides/vcs-hooks","https://moonrepo.dev/docs/config/workspace","https://moonrepo.dev/blog/moon-v1.9"],"evidence":"The vcs-hooks guide states: 'With moon, we provide a built-in solution for managing hooks, and syncing them across developers and machines.' Lefthook appears nowhere in official moonrepo documentation. Hook commands are declared under `vcs.hooks` as a map of kebab-cased Git hook names (e.g., `pre-commit`) to arrays of shell commands. The `vcs.sync` boolean (renamed from `vcs.syncHooks` in v2.0) enables automatic syncing without any external tool.","vote":"3-0 (merged from claims 0, 3, 7, 16)"},{"claim":"`moon sync vcs-hooks` does NOT install files into `.git/hooks/`. Instead it writes hook scripts to `.moon/hooks/` and sets `git config core.hooksPath` to point Git at that directory. This is a breaking change introduced in moon v2.0 — v1.x wrote directly to `.git/hooks/`.","confidence":"high","sources":["https://moonrepo.dev/docs/guides/vcs-hooks","https://moonrepo.dev/docs/migrate/2.0","https://moonrepo.dev/blog/moon-v2.0","https://github.com/moonrepo/moon/releases/tag/v2.0.0"],"evidence":"The vcs-hooks guide states: 'For Git, we set `core.hooksPath` to point to the `.moon/hooks` directory, which tells Git to look for hook scripts in that directory instead of the default `.git/hooks` directory.' The v2.0 migration guide explicitly documents the break: 'We no longer write hooks to the `.git/hooks` directory. Instead, all hooks are written to `.moon/hooks` and Git is configured to use this directory.' The v2.0 blog post confirms: 'Previously hooks were written to `.git/hooks` at the repository root, but we now don't write to `.git` at all, and entirely rely on `.moon/hooks` in each tree.' The migration guide warns users to manually remove old `.git/hooks` scripts left from v1.x.","vote":"3-0 (merged from claims 1, 4, 8, 10, 13, 14, 15)"},{"claim":"The `hookFormat` config option (under `vcs` in `.moon/workspace.yml`) accepts two values: `native` (default) and `bash`. `native` generates Bash `.sh` scripts on Unix and PowerShell `.ps1` scripts on Windows. `bash` forces Bash formatting on all operating systems.","confidence":"high","sources":["https://moonrepo.dev/docs/config/workspace","https://moonrepo.dev/docs/guides/vcs-hooks","https://github.com/moonrepo/moon/releases/tag/v1.29.0","https://moonrepo.dev/blog/moon-v1.29"],"evidence":"The workspace config docs state verbatim: `native` = 'The format native to the current operating system. Bash on Unix, and PowerShell on Windows.' and `bash` = 'Forces the format to Bash for all operating systems.' The vcs-hooks guide confirms: 'The `.moon/hooks` scripts are generated as Bash scripts (use a `.sh` file extension) on Unix, and PowerShell scripts (use a `.ps1` file extension) on Windows.' The v1.29.0 release note introduced the setting: 'Added a new setting `vcs.hookFormat` to `.moon/workspace.yml`, that can customize the shell/file format for hooks.' Note: one 2-1 claim suggested `.sh` extensions may have changed in newer versions, but the official docs still document them.","vote":"3-0 for format values (merged from claims 2, 9, 11, 12, 18, 21); claim 12 was 2-1 on .sh extension detail"},{"claim":"The `hookFormat` setting was introduced in moon v1.29.0. Prior to v1.29.0, there was no configurable hook format option. The vcs hooks feature itself was introduced earlier in v1.9.","confidence":"high","sources":["https://github.com/moonrepo/moon/releases/tag/v1.29.0","https://moonrepo.dev/docs/config/workspace","https://moonrepo.dev/blog/moon-v1.9"],"evidence":"The v1.29.0 release note marks the setting as 'new.' The workspace config docs annotate `hookFormat` as 'introduced in v1.29.0.' The v1.9 blog (which first introduced `vcs.hooks` and `vcs.syncHooks`) contains no mention of `hookFormat`, confirming it did not exist before v1.29.0.","vote":"3-0 (merged from claims 5, 21)"},{"claim":"The `vcs.hooks` setting maps kebab-cased Git hook names to arrays of arbitrary shell commands. These commands are embedded into the generated scripts in `.moon/hooks/`. Automatic syncing is controlled by `vcs.sync` (a boolean); in v2.0 this key was renamed from `vcs.syncHooks`.","confidence":"high","sources":["https://moonrepo.dev/docs/config/workspace","https://moonrepo.dev/docs/migrate/2.0","https://moonrepo.dev/blog/moon-v1.9"],"evidence":"The workspace config docs state: 'For Git, each hook name must be a valid kebab-cased name' and hooks map to 'a list of arbitrary commands to run within the hook script.' The v2.0 migration guide documents the rename: 'vcs.syncHooks → vcs.sync' under 'Renamed settings.' The v1.9 blog introduced the original `vcs.syncHooks` key: 'will automatically create and sync hooks with the local VCS checkout.'","vote":"3-0 (merged from claims 17, 19, 20)"},{"claim":"Moon v2.0 reworked the hooks layer for worktree compatibility as a breaking change. The migration guide notes users may need to manually remove old scripts from both `.git/hooks` and `.moon/hooks` when upgrading from v1.x.","confidence":"high","sources":["https://github.com/moonrepo/moon/releases/tag/v2.0.0","https://moonrepo.dev/docs/migrate/2.0","https://www.infoq.com/news/2026/05/moonrepo-2-release/"],"evidence":"The v2.0.0 release notes state: 'Reworked the hooks layer for better interoperability.' The migration guide explicitly calls this a breaking change and warns: 'We currently don't have an easy way to clean the previous implementation of hooks. You may need to manually remove the old scripts from .git/hooks and .moon/hooks if they are causing issues.' The InfoQ news article (secondary source) corroborates: 'a hooks system that no longer writes to `.git/hooks` directly.'","vote":"3-0 (merged from claims 6, 13)"}],"caveats":"One claim about `.sh` file extensions was 2-1 (not unanimous): a search snippet suggested `.sh` is no longer used in newer versions, but the official moonrepo.dev docs page still describes `.sh` for Unix Bash scripts. This may reflect a documentation lag or noise from an AI-generated snippet — the official docs are considered authoritative here. The `vcs.syncHooks` → `vcs.sync` rename detail (claim 17, 2-1) was resolved to high confidence because the v2.0 migration guide directly documents it; the 2-1 split was about naming precision in a secondary (blog) source, not the underlying fact. All primary sources are from moonrepo.dev official docs and GitHub releases; no independent third-party technical verification was performed. The InfoQ article (secondary source, one finding) was used only as corroboration, not as a primary claim source. Moon 2.
... (truncated 3829 chars, full result in /private/tmp/claude-501/-Users-gabe-Projects-oxc-react-codegen/4fe08305-17f4-42f0-993c-45f9fae2f7a4/tasks/w6arlas5y.output)</result>
<usage><agent_count>98</agent_count><subagent_tokens>1298714</subagent_tokens><tool_uses>710</tool_uses><duration_ms>564377</duration_ms></usage>
</task-notification>