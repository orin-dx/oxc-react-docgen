language: rust

tasks:
  build:
    command: cargo build -p oxc-react-docgen
    deps:
      - "core:build"
    options:
      runFromWorkspaceRoot: true
    inputs:
      - "src/**/*"
      - "Cargo.toml"

  test:
    command: cargo nextest run -p oxc-react-docgen --locked
    deps:
      - "core:build"
    options:
      runFromWorkspaceRoot: true
    inputs:
      - "src/**/*"
      - "tests/**/*"
      - "Cargo.toml"

  lint:
    command: cargo clippy -p oxc-react-docgen --locked -- -D warnings
    deps:
      - "core:build"
    options:
      runFromWorkspaceRoot: true
    inputs:
      - "src/**/*"
      - "Cargo.toml"
